services:
  app:
    build: .
    container_name: bpy_build
    # command: uvicorn src.main:app --host 0.0.0.0 --port 80 --reload
    command: gcc --version
    ports:
      - 80:80
    volumes:
      - .:/src